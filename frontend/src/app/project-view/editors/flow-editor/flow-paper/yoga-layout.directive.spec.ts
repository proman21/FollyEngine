import { YogaLayoutDirective } from './yoga-layout.directive';

describe('YogaLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new YogaLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
