import { AttributeActionDirective } from './attribute-action.directive';

describe('AttributeActionDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeActionDirective();
    expect(directive).toBeTruthy();
  });
});
